/**
 * Offline eval hook — mirror VJH discipline: N labeled days vs human summaries.
 * Wire your harness to load pairs from Oracle or JSON; this file stays dependency-free.
 *
 * Example: cosine similarity on embeddings, or LLM-as-judge — implement outside hot path.
 */
export function placeholderFidelityScore(_model: string, _human: string): number {
  return 0;
}
